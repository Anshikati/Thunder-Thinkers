name: needs_bridge
description: NeedsBridge - Smart Volunteer Coordination Platform for NGOs
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  provider: ^6.1.2
  go_router: ^13.2.1
  google_fonts: ^6.2.1
  image_picker: ^1.1.2
  animate_do: ^3.3.4
  intl: ^0.19.0
  cupertino_icons: ^1.0.6
  firebase_core: ^3.5.0
  cloud_firestore: ^5.4.3
  firebase_auth: ^5.3.1
  google_sign_in: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
